apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mariadb-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  storageClassName: fast
status: {}
